import React from "react";
import { Button } from "./ui/button";

function CreateNewSectionButton() {
  return (
    <Button variant="outline" className="h-10">
      + Add New Section
    </Button>
  );
}

export default CreateNewSectionButton;
